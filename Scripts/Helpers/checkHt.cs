using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class checkHt : MonoBehaviour
{
	void Start(){
	Debug.Log(gameObject.name);
	}
    // Start is called before the first frame update
    void Update(){
    Debug.Log(transform.localPosition.y); 
    }

    
    
}
